// Generated by apps/web/scripts/export-figma-library.mjs
// careerpilot-design-system-hash=616ca4ad02bc87a1c00d0fefd84133a9ad0d5f8cc6e78685d3e532505e081bce
// Intended for mcp__codex_apps__figma._use_figma with skillNames: "figma-use,figma-generate-library"

const DESIGN_SYSTEM = {
  "name": "CareerPilot APAC Design System",
  "version": "0.6.0",
  "locale": "zh-CN",
  "product": "CareerPilot APAC",
  "sourceOfTruth": {
    "css": "../src/app/globals.css",
    "figmaHandoff": true,
    "notes": "该文件是代码侧 Figma 变量、组件命名和可复用 UI 规范的源。"
  },
  "principles": [
    "优先服务日常操作：信息密度要高，但必须易扫读、适合重复使用。",
    "产品界面默认使用中文，技术命名只保留在组件、token 和 CSS 标识中。",
    "主操作必须清晰克制，禁止自动提交、破坏性或不可逆自动化。",
    "动效只用于定位、反馈和状态变化，不做无意义装饰。"
  ],
  "tokens": {
    "colors": [
      {
        "name": "bg/app",
        "css": "--color-bg-app",
        "value": "#f8fafc",
        "figmaName": "Color/BG/App"
      },
      {
        "name": "bg/surface",
        "css": "--color-bg-surface",
        "value": "#ffffff",
        "figmaName": "Color/BG/Surface"
      },
      {
        "name": "bg/subtle",
        "css": "--color-bg-subtle",
        "value": "#eef2f6",
        "figmaName": "Color/BG/Subtle"
      },
      {
        "name": "text/primary",
        "css": "--color-text-primary",
        "value": "#101722",
        "figmaName": "Color/Text/Primary"
      },
      {
        "name": "text/secondary",
        "css": "--color-text-secondary",
        "value": "#2f3a4a",
        "figmaName": "Color/Text/Secondary"
      },
      {
        "name": "text/muted",
        "css": "--color-text-muted",
        "value": "#647083",
        "figmaName": "Color/Text/Muted"
      },
      {
        "name": "border/subtle",
        "css": "--color-border-subtle",
        "value": "#d8dee8",
        "figmaName": "Color/Border/Subtle"
      },
      {
        "name": "border/strong",
        "css": "--color-border-strong",
        "value": "#b5c0ce",
        "figmaName": "Color/Border/Strong"
      },
      {
        "name": "accent/default",
        "css": "--color-accent-default",
        "value": "#087869",
        "figmaName": "Color/Accent/Default"
      },
      {
        "name": "accent/soft",
        "css": "--color-accent-soft",
        "value": "#ddf7f1",
        "figmaName": "Color/Accent/Soft"
      },
      {
        "name": "focus/default",
        "css": "--color-focus-default",
        "value": "#3578e5",
        "figmaName": "Color/Focus/Default"
      },
      {
        "name": "status/success",
        "css": "--color-status-success",
        "value": "#19a78e",
        "figmaName": "Color/Status/Success"
      },
      {
        "name": "status/info",
        "css": "--color-status-info",
        "value": "#3578e5",
        "figmaName": "Color/Status/Info"
      },
      {
        "name": "status/warning",
        "css": "--color-status-warning",
        "value": "#e59722",
        "figmaName": "Color/Status/Warning"
      },
      {
        "name": "status/danger",
        "css": "--color-status-danger",
        "value": "#e4574f",
        "figmaName": "Color/Status/Danger"
      },
      {
        "name": "ink/default",
        "css": "--color-ink",
        "value": "#101722",
        "figmaName": "Color/Ink/Default"
      },
      {
        "name": "ink/soft",
        "css": "--color-ink-soft",
        "value": "#172231",
        "figmaName": "Color/Ink/Soft"
      }
    ],
    "spacing": [
      {
        "name": "space/1",
        "css": "--spacing-1",
        "value": "4px",
        "figmaName": "Space/1"
      },
      {
        "name": "space/2",
        "css": "--spacing-2",
        "value": "8px",
        "figmaName": "Space/2"
      },
      {
        "name": "space/3",
        "css": "--spacing-3",
        "value": "12px",
        "figmaName": "Space/3"
      },
      {
        "name": "space/4",
        "css": "--spacing-4",
        "value": "16px",
        "figmaName": "Space/4"
      },
      {
        "name": "space/5",
        "css": "--spacing-5",
        "value": "20px",
        "figmaName": "Space/5"
      },
      {
        "name": "space/6",
        "css": "--spacing-6",
        "value": "24px",
        "figmaName": "Space/6"
      },
      {
        "name": "space/8",
        "css": "--spacing-8",
        "value": "32px",
        "figmaName": "Space/8"
      }
    ],
    "radii": [
      {
        "name": "radius/8",
        "css": "--radius-8",
        "value": "8px",
        "figmaName": "Radius/8"
      },
      {
        "name": "radius/12",
        "css": "--radius-12",
        "value": "12px",
        "figmaName": "Radius/12"
      },
      {
        "name": "radius/16",
        "css": "--radius-16",
        "value": "16px",
        "figmaName": "Radius/16"
      },
      {
        "name": "radius/24",
        "css": "--radius-24",
        "value": "24px",
        "figmaName": "Radius/24"
      },
      {
        "name": "radius/full",
        "css": "--radius-full",
        "value": "999px",
        "figmaName": "Radius/Full"
      }
    ],
    "shadows": [
      {
        "name": "shadow/surface",
        "css": "--shadow-surface",
        "value": "0 6px 18px -8px rgb(16 23 34 / 0.06)",
        "figmaName": "Shadow/Surface"
      },
      {
        "name": "shadow/panel",
        "css": "--shadow-panel",
        "value": "0 16px 36px -20px rgb(16 23 34 / 0.14)",
        "figmaName": "Shadow/Panel"
      }
    ],
    "motion": [
      {
        "name": "motion/surface-enter",
        "durationMs": 520,
        "easing": "cubic-bezier(0.16, 1, 0.3, 1)",
        "usage": "面板和卡片进入状态。"
      },
      {
        "name": "motion/control-hover",
        "durationMs": 220,
        "easing": "cubic-bezier(0.2, 0.8, 0.2, 1)",
        "usage": "按钮和导航悬停反馈。"
      },
      {
        "name": "motion/action-stagger",
        "durationMs": 68,
        "easing": "cubic-bezier(0.16, 1, 0.3, 1)",
        "usage": "高密度工作台的面板轻微错峰进入，用于降低页面切换时的视觉噪音。"
      },
      {
        "name": "motion/progress",
        "durationMs": 720,
        "easing": "cubic-bezier(0.16, 1, 0.3, 1)",
        "usage": "进度条和数值变化。"
      }
    ]
  },
  "typography": [
    {
      "name": "display/page-title",
      "size": "40px",
      "lineHeight": "1",
      "weight": 850,
      "mobileSize": "32px"
    },
    {
      "name": "display/metric",
      "size": "72px",
      "lineHeight": "0.95",
      "weight": 850,
      "mobileSize": "56px"
    },
    {
      "name": "heading/panel",
      "size": "19px",
      "lineHeight": "1.15",
      "weight": 800
    },
    {
      "name": "body/default",
      "size": "15px",
      "lineHeight": "1.7",
      "weight": 400
    },
    {
      "name": "label/control",
      "size": "12px",
      "lineHeight": "1.4",
      "weight": 780
    }
  ],
  "components": [
    {
      "name": "导航/侧栏",
      "figmaComponent": "CareerPilot/Nav Rail",
      "classes": [
        "nav-rail",
        "nav-list",
        "nav-link",
        "nav-dot"
      ],
      "states": [
        "默认",
        "选中",
        "悬停",
        "移动端网格",
        "移动端横滑"
      ],
      "usage": "产品主导航。桌面端使用侧栏，移动端收敛为紧凑横向标签导航。"
    },
    {
      "name": "框架/页头",
      "figmaComponent": "CareerPilot/Workbench Header",
      "classes": [
        "workbench-header"
      ],
      "states": [
        "桌面端",
        "移动端"
      ],
      "usage": "只承载页面标题和副标题。系统状态集中放在本地运行状态条中。"
    },
    {
      "name": "框架/流程定位",
      "figmaComponent": "CareerPilot/Workflow Ribbon",
      "classes": [
        "workflow-ribbon",
        "workflow-progress",
        "workflow-dots",
        "workflow-nav-button"
      ],
      "states": [
        "首项",
        "中间项",
        "末项",
        "桌面端",
        "移动端",
        "键盘焦点"
      ],
      "usage": "跨工作区的流程定位条，包含进度点和上一项/下一项导航。"
    },
    {
      "name": "框架/本地运行状态",
      "figmaComponent": "CareerPilot/Local Run Status",
      "classes": [
        "local-run-status",
        "local-run-copy",
        "local-run-grid",
        "local-run-card",
        "local-run-action"
      ],
      "states": [
        "就绪",
        "同步中",
        "需处理",
        "桌面网格",
        "平板网格",
        "移动横滑",
        "可复制"
      ],
      "usage": "共享本地操作状态条，展示日期、同步、今日计划、Edge 资产、备份记录，并支持复制 Markdown 状态。不得触发外部投递动作。"
    },
    {
      "name": "搜索/全局检索",
      "figmaComponent": "CareerPilot/Global Search",
      "classes": [
        "global-search-panel",
        "global-search-input",
        "global-search-count",
        "global-search-results",
        "global-search-empty"
      ],
      "states": [
        "空闲",
        "输入中",
        "有结果",
        "选中项",
        "空结果",
        "桌面网格",
        "移动堆叠",
        "键盘焦点"
      ],
      "usage": "本地跨记录检索岗位、投递、材料、简历、题库和数据源。结果只负责跳转到对应工作区，不触发外部自动化。"
    },
    {
      "name": "容器/面板",
      "figmaComponent": "CareerPilot/Panel",
      "classes": [
        "panel",
        "panel-title"
      ],
      "states": [
        "默认",
        "紧凑",
        "宽版"
      ],
      "usage": "工作台模块的主要重复内容容器。"
    },
    {
      "name": "操作/按钮",
      "figmaComponent": "CareerPilot/Button",
      "classes": [
        "fill-button",
        "secondary-button",
        "compact-button",
        "export-button"
      ],
      "states": [
        "主按钮",
        "次按钮",
        "导出按钮",
        "行内",
        "强调",
        "禁用",
        "悬停",
        "键盘焦点",
        "移动端两列"
      ],
      "usage": "命令型操作按钮。绿色用于主操作，蓝色用于导出/下载。任何按钮都不得自动触发最终岗位提交。"
    },
    {
      "name": "表单/输入",
      "figmaComponent": "CareerPilot/Form Field",
      "classes": [
        "inline-form",
        "editable-row",
        "form-panel",
        "ats-lab-grid"
      ],
      "states": [
        "默认",
        "聚焦",
        "必填",
        "需确认",
        "敏感"
      ],
      "usage": "用于本地资料、岗位导入、投递状态和 ATS 实验表单字段。"
    },
    {
      "name": "数据/卡片",
      "figmaComponent": "CareerPilot/Data Card",
      "classes": [
        "market-card",
        "source-card",
        "material-card",
        "application-card",
        "package-snapshot-card"
      ],
      "states": [
        "默认",
        "成功",
        "信息",
        "提醒",
        "危险",
        "空状态"
      ],
      "usage": "重复展示岗位、数据源、材料、投递和表单快照记录。"
    },
    {
      "name": "数据/公开来源同步",
      "figmaComponent": "CareerPilot/Source Sync",
      "classes": [
        "source-sync-panel",
        "source-sync-grid",
        "source-sync-actions",
        "latest-sync-log",
        "source-sync-result-list",
        "source-sync-result"
      ],
      "states": [
        "未配置",
        "可同步",
        "同步中",
        "成功",
        "部分失败",
        "跳过",
        "桌面四列",
        "移动堆叠"
      ],
      "usage": "数据源页的公开 URL 同步控制台。只抓取用户配置的公开页面，显示新增、去重、跳过和失败，不登录、不绕限制、不投递。"
    },
    {
      "name": "今日/岗位刷新",
      "figmaComponent": "CareerPilot/Daily Source Refresh",
      "classes": [
        "daily-refresh-panel",
        "daily-refresh-grid",
        "daily-refresh-actions",
        "daily-refresh-list",
        "daily-refresh-source",
        "daily-refresh-empty"
      ],
      "states": [
        "未配置",
        "待刷新",
        "今日已同步",
        "刷新中",
        "有新增",
        "移动堆叠"
      ],
      "usage": "今日打卡页的每日岗位刷新入口，把公开来源同步放到生成短名单之前。只更新本地岗位库，不登录、不自动投递。"
    },
    {
      "name": "状态/标签",
      "figmaComponent": "CareerPilot/Status Chip",
      "classes": [
        "soft-pill",
        "status-chip",
        "answer-chip",
        "chip-list"
      ],
      "states": [
        "中性",
        "成功",
        "信息",
        "提醒",
        "危险",
        "强调"
      ],
      "usage": "小型状态标签。移动端必须自然换行，不能裁切中文。"
    },
    {
      "name": "反馈/空状态",
      "figmaComponent": "CareerPilot/Empty State",
      "classes": [
        "empty-state",
        "empty-state-icon",
        "empty-state-action"
      ],
      "states": [
        "中性",
        "信息",
        "提醒",
        "成功",
        "危险",
        "可操作"
      ],
      "usage": "统一展示无数据、下一步受阻和可恢复反馈状态。"
    },
    {
      "name": "ATS/实验表单",
      "figmaComponent": "CareerPilot/ATS Lab",
      "classes": [
        "ats-lab-page",
        "ats-lab-tabs",
        "ats-lab-panel",
        "ats-lab-grid"
      ],
      "states": [
        "Workday",
        "Greenhouse",
        "Lever",
        "SmartRecruiters",
        "Ashby",
        "BambooHR",
        "Generic"
      ],
      "usage": "Edge 扩展字段识别和安全边界的可控测试页面，覆盖常见海外 ATS 和通用公司官网表单。"
    },
    {
      "name": "填表/映射学习",
      "figmaComponent": "CareerPilot/Mapping Learning",
      "classes": [
        "mapping-learning-panel",
        "mapping-learning-metrics",
        "mapping-suggestion-strip",
        "field-map-grid"
      ],
      "states": [
        "空状态",
        "建议",
        "已保存",
        "安全批量",
        "仅确认",
        "移动堆叠"
      ],
      "usage": "字段映射学习队列，用于把 Edge 表单快照转成可复用填表规则。批量动作只允许安全字段，绝不提交申请。"
    },
    {
      "name": "填表/快照回放",
      "figmaComponent": "CareerPilot/Snapshot Replay",
      "classes": [
        "snapshot-replay-panel",
        "snapshot-replay-head",
        "snapshot-replay-metrics",
        "snapshot-replay-groups",
        "snapshot-replay-group"
      ],
      "states": [
        "可直接填写",
        "待人工确认",
        "缺映射/缺值",
        "已保存规则",
        "桌面三列",
        "移动堆叠",
        "可复制",
        "可下载"
      ],
      "usage": "把 Edge 表单快照转成提交前回放清单，展示安全可填、人工确认和缺口字段。复制和下载只导出本地 Markdown，不触发外部表单提交。"
    },
    {
      "name": "投递/管线复盘",
      "figmaComponent": "CareerPilot/Pipeline Review",
      "classes": [
        "pipeline-review-panel",
        "pipeline-review-metrics",
        "pipeline-review-next",
        "market-funnel-list",
        "market-funnel-row"
      ],
      "states": [
        "空状态",
        "健康",
        "过期",
        "市场漏斗",
        "移动堆叠"
      ],
      "usage": "投递管线复盘界面，展示回信率、面试进度、过期跟进和市场分布。复制操作只导出本地 Markdown。"
    }
  ],
  "qualityGates": [
    "字体大小不得使用视口单位，也不得使用基于视口的 clamp 缩放。",
    "字距必须为 0 或省略。",
    "规范中引用的每个 CSS 变量都必须存在于 :root。",
    "工作台和 ATS 实验表单必须通过桌面端、移动端视觉烟测。",
    "可见按钮和链接必须具备文字、title 或 aria-label。",
    "交互控件必须提供可见的 focus-visible 状态。",
    "工作台必须提供键盘跳转到主内容区的跳过链接。",
    "全局搜索必须出现在每个工作区，并在桌面端、移动端通过输入查询验证。",
    "全局搜索必须支持键盘聚焦、方向键选择结果和回车打开当前结果，且不在 UI 中暴露快捷键说明。",
    "本地运行状态必须出现在每个工作区，包含五张状态卡和复制动作。",
    "公开来源同步必须在数据源页展示同步指标、单来源同步动作和只访问公开 URL 的边界说明。",
    "每日岗位刷新必须在今日打卡页展示来源同步状态、刷新动作和数据源管理入口。",
    "字段映射学习必须在桌面端、移动端展示队列指标和仅安全字段的批量保存动作。",
    "快照回放必须在投递包和填表资料库中展示字段分类、复制动作和 Markdown 下载动作。",
    "投递管线复盘必须在桌面端、移动端展示漏斗指标、市场行和本地 Markdown 复制动作。",
    "CSV 导出按钮必须使用 export-button 变体，并在桌面端、移动端通过真实下载验证。",
    "动效必须尊重用户的减少动态效果偏好。",
    "填表 UI 改动完成前，Edge 扩展烟测必须通过。"
  ]
};
const RUN_ID = "careerpilot-apac-ds-" + Date.now();
const createdNodeIds = [];
const mutatedNodeIds = [];

function remember(node, phase, key) {
  node.setSharedPluginData("careerpilot", "run_id", RUN_ID);
  node.setSharedPluginData("careerpilot", "phase", phase);
  node.setSharedPluginData("careerpilot", "key", key);
  mutatedNodeIds.push(node.id);
  return node;
}

function hexToRgb(hex) {
  const clean = String(hex).replace("#", "");
  const value = Number.parseInt(clean, 16);
  return {
    r: ((value >> 16) & 255) / 255,
    g: ((value >> 8) & 255) / 255,
    b: (value & 255) / 255
  };
}

function parsePixels(value) {
  return Number.parseFloat(String(value).replace("px", ""));
}

function variableScopeForToken(group, token) {
  if (group === "colors") {
    if (token.name.startsWith("text/")) return ["TEXT_FILL"];
    if (token.name.startsWith("border/")) return ["STROKE_COLOR"];
    return ["FRAME_FILL", "SHAPE_FILL"];
  }
  if (group === "spacing") return ["GAP"];
  if (group === "radii") return ["CORNER_RADIUS"];
  return [];
}

function variableTypeForGroup(group) {
  return group === "colors" ? "COLOR" : "FLOAT";
}

function valueForToken(group, token) {
  return group === "colors" ? hexToRgb(token.value) : parsePixels(token.value);
}

async function getOrCreateCollection(name) {
  const existing = (await figma.variables.getLocalVariableCollectionsAsync()).find((collection) => collection.name === name);
  if (existing) return existing;

  const collection = figma.variables.createVariableCollection(name);
  if (collection.modes?.[0]) {
    collection.renameMode(collection.modes[0].modeId, "Value");
  }
  return collection;
}

async function getVariableInCollection(collection, name) {
  const variables = await Promise.all(collection.variableIds.map((id) => figma.variables.getVariableByIdAsync(id)));
  return variables.find((variable) => variable?.name === name) || null;
}

async function createTokens() {
  const collections = {};
  const variables = {};

  for (const [group, items] of Object.entries(DESIGN_SYSTEM.tokens)) {
    if (group === "motion" || group === "shadows") continue;

    const collection = await getOrCreateCollection("CareerPilot / " + group);
    const modeId = collection.modes[0].modeId;
    collections[group] = collection.id;

    for (const token of items) {
      const name = token.figmaName.replaceAll("/", " / ");
      const type = variableTypeForGroup(group);
      const existing = await getVariableInCollection(collection, name);
      const variable = existing || figma.variables.createVariable(name, collection, type);

      variable.setValueForMode(modeId, valueForToken(group, token));
      variable.scopes = variableScopeForToken(group, token);
      if (token.css) {
        variable.setVariableCodeSyntax("WEB", "var(" + token.css + ")");
      }

      variables[token.figmaName] = variable.id;
      if (!existing) createdNodeIds.push(variable.id);
    }
  }

  return { collections, variables };
}

async function createTextStyles() {
  await figma.loadFontAsync({ family: "Inter", style: "Regular" });
  const localStyles = figma.getLocalTextStyles();
  const styles = {};

  for (const item of DESIGN_SYSTEM.typography) {
    let style = localStyles.find((candidate) => candidate.name === "CareerPilot / " + item.name);
    if (!style) {
      style = figma.createTextStyle();
      style.name = "CareerPilot / " + item.name;
    }

    const size = parsePixels(item.size);
    style.fontName = { family: "Inter", style: "Regular" };
    style.fontSize = size;
    style.lineHeight = item.lineHeight === "1" ? { unit: "PIXELS", value: size } : { unit: "PIXELS", value: Math.round(size * Number.parseFloat(item.lineHeight)) };
    style.letterSpacing = { unit: "PIXELS", value: 0 };
    style.description = "CareerPilot APAC " + item.name;
    styles[item.name] = style.id;
  }

  return styles;
}

function shadowEffectFromToken(token) {
  const match = token.value.match(/0\s+([\d.]+)px\s+([\d.]+)px\s+(-?[\d.]+)px\s+rgb\((\d+)\s+(\d+)\s+(\d+)\s+\/\s+([\d.]+)\)/);
  if (!match) return [];
  return [
    {
      type: "DROP_SHADOW",
      visible: true,
      blendMode: "NORMAL",
      color: {
        r: Number(match[5]) / 255,
        g: Number(match[6]) / 255,
        b: Number(match[7]) / 255,
        a: Number(match[8])
      },
      offset: { x: 0, y: Number(match[1]) },
      radius: Number(match[2]),
      spread: Number(match[3])
    }
  ];
}

function createEffectStyles() {
  const localStyles = figma.getLocalEffectStyles();
  const styles = {};

  for (const token of DESIGN_SYSTEM.tokens.shadows) {
    let style = localStyles.find((candidate) => candidate.name === "CareerPilot / " + token.figmaName);
    if (!style) {
      style = figma.createEffectStyle();
      style.name = "CareerPilot / " + token.figmaName;
    }
    style.effects = shadowEffectFromToken(token);
    style.description = token.css + " = " + token.value;
    styles[token.figmaName] = style.id;
  }

  return styles;
}

async function getOrCreatePage(name) {
  let page = figma.root.children.find((candidate) => candidate.name === name);
  if (!page) {
    page = figma.createPage();
    page.name = name;
    createdNodeIds.push(page.id);
  }
  remember(page, "page", name);
  return page;
}

async function addText(parent, text, size, color, width) {
  const node = figma.createText();
  await figma.loadFontAsync({ family: "Inter", style: "Regular" });
  node.fontName = { family: "Inter", style: "Regular" };
  node.characters = text;
  node.fontSize = size;
  node.lineHeight = { unit: "PIXELS", value: Math.round(size * 1.35) };
  node.fills = [{ type: "SOLID", color }];
  node.textAutoResize = "HEIGHT";
  node.resize(width, node.height);
  parent.appendChild(node);
  createdNodeIds.push(node.id);
  return node;
}

async function createSectionFrame(page, name, x, y, width) {
  const frame = figma.createAutoLayout("VERTICAL", {
    name,
    itemSpacing: 16,
    paddingTop: 24,
    paddingRight: 24,
    paddingBottom: 24,
    paddingLeft: 24
  });
  frame.resize(width, 10);
  frame.x = x;
  frame.y = y;
  frame.fills = [{ type: "SOLID", color: hexToRgb("#ffffff") }];
  frame.strokes = [{ type: "SOLID", color: hexToRgb("#d8dee8") }];
  frame.cornerRadius = 16;
  page.appendChild(frame);
  createdNodeIds.push(frame.id);
  remember(frame, "documentation", name);
  return frame;
}

async function buildDocumentationPages() {
  const cover = await getOrCreatePage("Cover");
  await figma.setCurrentPageAsync(cover);
  const coverFrame = await createSectionFrame(cover, "CareerPilot APAC / Cover", 80, 80, 760);
  await addText(coverFrame, "CareerPilot APAC Design System", 34, hexToRgb("#101722"), 680);
  await addText(coverFrame, "Local-first job search operating system UI library. Source of truth: code tokens, Chinese UI, safe automation boundaries.", 15, hexToRgb("#647083"), 680);

  const foundations = await getOrCreatePage("Foundations / Tokens");
  await figma.setCurrentPageAsync(foundations);
  const colorFrame = await createSectionFrame(foundations, "Color Tokens", 80, 80, 880);
  await addText(colorFrame, "Color Tokens", 26, hexToRgb("#101722"), 780);
  for (const token of DESIGN_SYSTEM.tokens.colors) {
    await addText(colorFrame, token.figmaName + "  " + token.css + "  " + token.value, 13, hexToRgb("#2f3a4a"), 780);
  }

  const scaleFrame = await createSectionFrame(foundations, "Spacing / Radius", 1000, 80, 520);
  await addText(scaleFrame, "Spacing + Radius", 26, hexToRgb("#101722"), 440);
  for (const token of [...DESIGN_SYSTEM.tokens.spacing, ...DESIGN_SYSTEM.tokens.radii]) {
    await addText(scaleFrame, token.figmaName + "  " + token.value, 13, hexToRgb("#2f3a4a"), 440);
  }
}

async function buildComponentPages() {
  const page = await getOrCreatePage("Components");
  await figma.setCurrentPageAsync(page);

  let x = 80;
  let y = 80;
  for (const component of DESIGN_SYSTEM.components) {
    const card = await createSectionFrame(page, component.figmaComponent, x, y, 360);
    await addText(card, component.figmaComponent, 20, hexToRgb("#101722"), 300);
    await addText(card, component.usage, 13, hexToRgb("#647083"), 300);
    await addText(card, "States: " + component.states.join(", "), 12, hexToRgb("#087869"), 300);
    await addText(card, "Classes: " + component.classes.map((item) => "." + item).join("  "), 11, hexToRgb("#647083"), 300);

    const componentNode = figma.createComponent();
    componentNode.name = component.figmaComponent;
    componentNode.resize(320, 88);
    componentNode.fills = [{ type: "SOLID", color: hexToRgb("#f8fafc") }];
    componentNode.strokes = [{ type: "SOLID", color: hexToRgb("#d8dee8") }];
    componentNode.cornerRadius = 12;
    componentNode.setSharedPluginData("careerpilot", "key", component.name);
    card.appendChild(componentNode);
    createdNodeIds.push(componentNode.id);

    x += 420;
    if (x > 1340) {
      x = 80;
      y += 430;
    }
  }
}

async function main() {
  const tokenSummary = await createTokens();
  const textStyles = await createTextStyles();
  const effectStyles = createEffectStyles();
  await buildDocumentationPages();
  await buildComponentPages();

  return {
    ok: true,
    runId: RUN_ID,
    product: DESIGN_SYSTEM.product,
    version: DESIGN_SYSTEM.version,
    tokenCount: Object.values(DESIGN_SYSTEM.tokens).flat().length,
    componentCount: DESIGN_SYSTEM.components.length,
    tokenSummary,
    textStyleCount: Object.keys(textStyles).length,
    effectStyleCount: Object.keys(effectStyles).length,
    createdNodeIds,
    mutatedNodeIds
  };
}

return await main();
